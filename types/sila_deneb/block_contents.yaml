SilaDeneb:
  KZGProofs:
    type: array
    items:
      $ref: '../primitive.yaml#/KZGProof'
    minItems: 0
    maxItems: 4096

  Blobs:
    type: array
    items:
      $ref: "../primitive.yaml#/Blob"
    minItems: 0
    maxItems: 4096
  
  BlockContents:
    type: object
    description: "The required object for block production according to the SilaDeneb CL spec."
    required: [block, kzg_proofs, blobs]
    properties:
      block:
        $ref: "./block.yaml#/SilaDeneb/BeaconBlock"
      kzg_proofs:
        $ref: "#/SilaDeneb/KZGProofs"
      blobs:
        $ref: "#/SilaDeneb/Blobs"

  SignedBlockContents:
    type: object
    description: "The required signed components of block production according to the SilaDeneb CL spec."
    required: [signed_block, kzg_proofs, blobs]
    properties:
      signed_block:
        $ref: "./block.yaml#/SilaDeneb/SignedBeaconBlock"
      kzg_proofs:
        $ref: "#/SilaDeneb/KZGProofs"
      blobs:
        $ref: "#/SilaDeneb/Blobs"
